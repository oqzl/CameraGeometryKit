# Architecture

CameraGeometryKit は1つの canonical image coordinate space を中心に、framework boundary ごとの変換を明示する小さな基盤です。

geometry、frame metadata、rotation/mirroring policy、generation token、diagnostics を共通化し、product effect、persistence、app固有UIは上位に残します。

詳細は各サブドキュメントを参照してください。
